[tool.ruff]
line-length = 120
target-version = "py311"
exclude = [
    ".git",
    ".venv",
    "venv",
    "__pycache__",
]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
exclude = [
    "venv/",
]

[tool.python-doctor]
profile = "script"
