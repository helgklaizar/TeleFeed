---
description: Старт сессии — выполнять всегда
---
1. Читать `project-info.md`.
2. Читать локальный `GEMINI.md` (оперативная память прошлой сессии).
3. Проверить `.agent/workflows/` — есть ли воркфлоу для текущей задачи.
4. OpenMemory: запросить через MCP — что уже делали в этом проекте или по похожей теме.

## Выбор подхода
- Есть навык в `/Users/klai/.gemini/antigravity/skills/` → `view_file` + следовать.
- Простая задача → решать напрямую.
- Сложная → использовать `tools/` из `/Users/klai/.gemini/tools/`.
- Повторяемая → создать воркфлоу в `.agent/workflows/`.
